{
  "name": "db",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "start": "node --env-file=../.env dist/index.js",
    "dev": "npm run build && npm run start",
    "seed:db": "npm run build && node --env-file=../.env dist/seed-db.js",
    "refresh:views": "npm run build && node --env-file=../.env dist/cron.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/pg": "^8.11.6",
    "pg": "^8.12.0",
    "redis": "^4.6.14"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
